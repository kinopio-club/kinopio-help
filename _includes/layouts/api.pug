---
layout: default.pug
---

// colors match api h2s in styles.styl
- items = [ {name: 'All', link: '#all', color: 'khaki'}, {name: 'Users', link: '#users', color: 'lightpink'}, {name: 'Spaces', link: '#spaces', color: 'cyan'}, {name: 'Cards', link: '#cards', color: 'violet'}, {name: 'Connections', link: '#connections', color: 'tomato'}, {name: 'Connection Types', link: '#connection-types', color: 'chocolate'} ]

ul.api-contents
  each item in items
    li
      a(href=item.link style=`background: ${item.color}`)
        span #{item.name}

article.api
  h1 #{title}
  span !{content}
  script(src="/assets/api.js")
