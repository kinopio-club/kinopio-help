---
layout: default.pug
---
h1 Hello

p
  a(href="https://kinopio.club") Kinopio
  span &nbsp;is the visual thinking tool for new ideas and hard problems.

p By making it easy to get your thoughts out and form connections organically, Kinopio works how our brains work.

include /contact.pug

mixin posts(collection)
  h2 #{collection}
  ul
    each post in collections[collection]
      li
        a(href=post.url style={background: post.data.color})
          span #{post.data.title}


section
  - var collection = 'How to Use'
  +posts(collection)

  - var collection = 'About Kinopio'
  +posts(collection)

  - var collection = 'API'
  +posts(collection)

  - var collection = 'Collaboration'
  +posts(collection)

  - var collection = 'Explore and Favorites'
  +posts(collection)

  - var collection = 'Importing and Exporting'
  +posts(collection)

  - var collection = 'Policies'
  +posts(collection)

  - var collection = 'Press'
  +posts(collection)

  - var collection = 'Sharing and Privacy'
  +posts(collection)

  - var collection = 'User Settings'
  +posts(collection)
